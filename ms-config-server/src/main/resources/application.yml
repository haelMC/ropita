
server:
  port: 7070
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/haelMC/ropita.git
          #username:
          #password:
          search-paths: config-data
          default-label: main
  security:
    user:
      name: root
      password: 1234567